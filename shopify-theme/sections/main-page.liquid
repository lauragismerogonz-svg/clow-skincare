<div class="page-width" style="padding: 5rem 1.5rem; max-width: 800px">
  <h1 style="font-size: clamp(1.8rem,4vw,2.8rem); margin-bottom: 2rem">{{ page.title }}</h1>
  <div class="rte" style="line-height:1.75;font-size:1.05rem">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page" }
{% endschema %}
